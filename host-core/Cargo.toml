[package]
name = "nodalmerge-host-core"
version = "0.1.0"
edition = "2021"
description = "Host-neutral orchestration surface for NodalMerge"
readme = "README.md"
keywords = ["crdt", "sync", "host", "orchestration"]
categories = ["network-programming", "api-bindings"]

[dependencies]
nodalmerge-core = { version = "0.1.0", path = "../core" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
ed25519-dalek = { workspace = true }
