# nodalmerge-host-ffi

C ABI wrapper for `nodalmerge-host-core`.

## Scope

- stable C-callable entry points
- binary command/event payload boundary
- host language interop support (.NET and others)

Native artifacts from this crate are packaged into RID-specific .NET native packages.
