{
  "artifact": "query-wave3-backpressure-run03",
  "recorded_at": "2026-05-27T23:45:00.0000000-05:00",
  "wave": 3,
  "track": "FSE-03 scheduler/backpressure (fair bounded FIFO queue)",
  "status": "recorded",
  "source_plan": "docs/QUERY_MATERIALIZATION_EXECUTION_PLAN.md",
  "increment": {
    "env": [
      "NODALMERGE_QUERY_BUILD_MAX_INFLIGHT",
      "NODALMERGE_QUERY_BUILD_MAX_QUEUE",
      "NODALMERGE_QUERY_BUILD_MAX_ROWS"
    ],
    "metrics": [
      "nodalmerge_query_build_queue_depth",
      "nodalmerge_query_build_queued_total",
      "nodalmerge_query_build_queue_wait_seconds"
    ],
    "behavior": "try_acquire then bounded FIFO wait via per-room Semaphore; reject with reject.query_backpressure when wait queue is full",
    "deterministic_vectors": [
      "projection_build_waits_in_fifo_order_then_succeeds",
      "projection_build_rejects_when_wait_queue_is_full",
      "projection_build_rejects_when_row_limit_exceeded"
    ]
  },
  "verification": {
    "commands": [
      "cargo test -p nodalmerge-server query_control::tests -- --nocapture"
    ],
    "result": "pass"
  },
  "notes": [
    "Slice 3 replaces immediate reject-on-contention with fair queueing up to max_queue waiters.",
    "max_queue=0 disables waiting and rejects when inflight slots are saturated."
  ]
}
