[package]
name = "nodalmerge-postgres-store"
version = "0.1.0"
edition = "2021"
publish = false
description = "Compatibility wrapper crate re-exporting activesync-postgres-store during rename migration"
license = "MIT OR Apache-2.0"

[dependencies]
activesync-postgres-store = { path = "../../node-stores/postgres" }
