[package]
name = "activesync-gc"
version = "0.1.0"
edition = "2021"
description = "Host-neutral GC contracts and coordinator primitives for ActiveSync CAS blobs"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
