[package]
name = "activesync-postgres-store"
version = "0.1.0"
edition = "2021"
publish = false
description = "Compatibility wrapper crate re-exporting nodalmerge-postgres-store during rename migration"
license = "MIT OR Apache-2.0"

[dependencies]
nodalmerge-postgres-store = { path = "../../node-stores/postgres" }
