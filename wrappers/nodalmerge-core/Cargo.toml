[package]
name = "nodalmerge-core"
version = "0.1.0"
edition = "2021"
description = "Compatibility wrapper crate re-exporting activesync-core during rename migration"
license = "MIT OR Apache-2.0"

[dependencies]
activesync-core = { version = "0.1.0", path = "../../core" }
