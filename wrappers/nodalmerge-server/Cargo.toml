[package]
name = "activesync-server"
version = "0.1.0"
edition = "2021"
description = "Compatibility wrapper crate re-exporting nodalmerge-server during rename migration"
license = "MIT OR Apache-2.0"

[dependencies]
nodalmerge-server = { version = "0.1.0", path = "../../server" }
