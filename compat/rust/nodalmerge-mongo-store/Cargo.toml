[package]
name = "activesync-mongo-store"
version = "0.1.0"
edition = "2021"
publish = false
description = "Compatibility wrapper crate re-exporting nodalmerge-mongo-store during rename migration"
license = "MIT OR Apache-2.0"

[dependencies]
nodalmerge-mongo-store = { path = "../../../server/stores/mongo" }
