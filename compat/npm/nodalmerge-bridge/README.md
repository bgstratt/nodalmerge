# nodalmerge-bridge

This package is a compatibility wrapper that re-exports `activesync-bridge` during the ActiveSync -> NodalMerge migration window.

Install:

```bash
npm install nodalmerge-bridge
```
