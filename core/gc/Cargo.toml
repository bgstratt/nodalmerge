[package]
name = "nodalmerge-gc"
version = "0.2.0"
edition = "2021"
description = "Host-neutral GC contracts and coordinator primitives for NodalMerge CAS blobs"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
