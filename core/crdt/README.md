# nodalmerge-core

Deterministic core runtime for NodalMerge replication.

## Scope

- state graph and CRDT merge/resolution
- replay and canonical hash computation
- policy and token primitives
- compact binary pack/unpack and handshake helpers

This crate is the open runtime foundation used by host adapters and bridges.
