# nodalmerge-sdk-js

This package is a compatibility wrapper that re-exports `activesync-sdk-js` during the ActiveSync -> NodalMerge migration window.

Install:

```bash
npm install nodalmerge-sdk-js
```
