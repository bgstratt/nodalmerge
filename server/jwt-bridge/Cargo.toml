[package]
name = "nodalmerge-jwt-bridge"
version = "0.1.0"
edition = "2021"
description = "Trusted-issuer JWT → NodalMerge RoomToken minting bridge"

[dependencies]
nodalmerge-core = { version = "0.1.0", path = "../../core/crdt" }
nodalmerge-capability-profile = { version = "0.1.0", path = "../capability-profile" }
ed25519-dalek = { workspace = true }
jsonwebtoken = "9"
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
rand = "0.8"
